# Vite picks this up at build time. Set in Vercel project env.
# Empty = use Vite proxy (local dev). For prod, set to your Render server URL.
VITE_API_BASE_URL=https://passportcard-demo-server.onrender.com
